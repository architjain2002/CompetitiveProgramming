#include <iostream>
int main(int argc, char const *argv[])
{
    //! question def
    // here tetrahedron is given with an ant at "D" and we need to find the number of ciculations of the ant that
    //    in n length given

    // input --- n
    // output --- cycles
    int n = 0;
    while (n != 0)
    {

        n = n - 1;
    }
    return 0;
}
